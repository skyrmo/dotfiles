# dotfiles

```bash
xcode-select --install

git clone https://github.com/skyrmo/dotfiles ~/.dotfiles

cd ~/.dotfiles

git remote remove origin

git remote add origin git@github.com:skyrmo/dotfiles.git

# run setup script
bash run.sh

```
